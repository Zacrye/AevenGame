using UnityEngine;
using System.Collections;
using AssemblyCSharp;

public class JsonTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//JsonSerializer.Serialize(Character, new Character());
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
